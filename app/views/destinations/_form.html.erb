<%= simple_form_for(@destination) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.collection_select( :FeedId, Feed.all, :id, :Title, prompt:true ) %>
    <%= f.input :Name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
