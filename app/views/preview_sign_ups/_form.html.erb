<%= simple_form_for(@preview_sign_up) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :EmailAddress %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
