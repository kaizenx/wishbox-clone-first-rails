<%= simple_form_for(@user_in_feed) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :userid %>
    <%= f.input :feedid %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
