<h1>Listing user_in_feeds</h1>

<table>
  <thead>
    <tr>
      <th>Userid</th>
      <th>Feedid</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @user_in_feeds.each do |user_in_feed| %>
      <tr>
        <td><%= user_in_feed.userid %></td>
        <td><%= user_in_feed.feedid %></td>
        <td><%= link_to 'Show', user_in_feed %></td>
        <td><%= link_to 'Edit', edit_user_in_feed_path(user_in_feed) %></td>
        <td><%= link_to 'Destroy', user_in_feed, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User in feed', new_user_in_feed_path %>
